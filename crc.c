#include<stdio.h>
#include<string.h>

void main()
{
	int i,j,lg,lm;
	char m[100],q[100],r[30],g[30],g1[30];
	printf("Enter the binary form of M(x): ");
	gets(m);
	printf("Enter data in binary form of g(x): ");
	get(g)
	lg=strlen(g);
	lm=strlen(m);
	strcpy(lg,g);
	
}